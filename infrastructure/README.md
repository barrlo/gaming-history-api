# Infrastructure boundary

This API repository will own all Terraform, including UI storage/CDN, API hosting, three DynamoDB tables, scheduling, Step Functions, IAM and GitHub OIDC. No deployable resources exist at this scaffold checkpoint. AWS region remains us-east-1. Deployment workflow and cost review come with the infrastructure checkpoint; CI currently only validates code.
